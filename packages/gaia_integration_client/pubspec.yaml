name: gaia_integration_client
description: Reusable GAIA integration client for dashboard and local tools
version: 0.7.0
publish_to: "none"

environment:
  sdk: ^3.11.5

dependencies:
  http: ^1.5.0

dev_dependencies:
  test: ^1.25.8
