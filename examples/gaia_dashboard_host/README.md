# GAIA Dashboard Host

This example Flutter app embeds the reusable `gaia_dashboard_module` package and the
`gaia_integration_client` contract client.

It is intended as a reference host for:

- module integration checks
- trust and compatibility surface validation
- example-only UI verification in CI

The host is read-mostly by design and does not ship any production backend logic.
