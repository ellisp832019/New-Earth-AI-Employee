# Known Limitations

- Governance Context depends on a reachable NEOS governance endpoint for live data.
- When the endpoint is unavailable, GAIA falls back to UNKNOWN/unavailable semantics.
- Local cache records do not replace live NEOS truth.
- C6 remains intentionally out of scope.
